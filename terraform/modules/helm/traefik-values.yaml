ingressRoute:
  dashboard:
    enabled: true
    matchRule: Host(`dashboard.localhost`) # only for accessing the traefik dashboard
    entryPoints:
      - web
providers:
  kubernetesGateway:
    enabled: false
